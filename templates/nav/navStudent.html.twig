<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <a class="navbar-brand font-weight-bold" href="{{ path('home') }}">Mon Ecole</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('dashboard.index') }}">
                    <i class="fas fa-home px-1"></i>
                    <span class="d-lg-none">Accueil</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link align-middle" href="{{ path('admin.message.index') }}">
                    <span class="badge badge-danger"></span>
                    <i class="fas fa-envelope px-1"></i>
                    <span class="d-lg-none">Messagerie</span>
                </a>
            </li>
            <li class="nav-item">
                <span id="separator" class="nav-link text-white">|</span>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link cream font-weight-bold" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                {{ app.user.firstname ~ ' ' ~ app.user.lastname }}
                <i class="fas fa-sign-out-alt"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('logout') }}">se deconnecter</a>
                </div>
            </li>
        </ul>
    </div>
</nav>
